{
  "entities": {
    "IndustrialCategory": {
      "title": "Industrial Category",
      "description": "An industrial category with search keywords and classified sector.",
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "Unique category identifier" },
        "keyword": { "type": "string", "description": "Keyword or phrasing to match user inputs" },
        "categoryName": { "type": "string", "description": "Name of the industry sector or group" },
        "subCategory": { "type": "string", "description": "Sub-category details" },
        "updatedBy": { "type": "string", "description": "Email of admin who updated this record" },
        "updatedAt": { "type": "string", "format": "date-time", "description": "Timestamp of the last update" }
      },
      "required": ["id", "keyword", "categoryName", "updatedAt"]
    },
    "AdminProfile": {
      "title": "Admin Profile",
      "description": "User profile for authenticated administrators from the @bu.ac.th domain.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "Firebase Authentication user ID" },
        "email": { "type": "string", "description": "User email address ending in @bu.ac.th" },
        "displayName": { "type": "string", "description": "Full name of the administrator" },
        "photoURL": { "type": "string", "description": "Profile picture URL" },
        "role": { "type": "string", "enum": ["admin"], "description": "Assigned role, strictly admin" },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp when admin profile was created" }
      },
      "required": ["uid", "email", "role", "createdAt"]
    }
  },
  "firestore": {
    "/industrial_categories/{categoryId}": {
      "schema": "IndustrialCategory",
      "description": "Master list of industrial categorizations matched by keywords."
    },
    "/admins/{adminId}": {
      "schema": "AdminProfile",
      "description": "Administrators allowed to manage the database, saved for data privacy."
    }
  }
}
